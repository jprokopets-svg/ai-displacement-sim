{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://ai-displacement-sim-production.up.railway.app/api/:path*" }
  ]
}
